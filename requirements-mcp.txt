# MCP 서버용 가상환경 (.venv310). 설계서 §2 "기술 스택".
# py -3.10 -m venv .venv310
# .venv310/Scripts/python -m pip install -r requirements-mcp.txt

tensorflow==2.15.1   # Keras 2. SavedModel 직접 적재 (정정 I). 2.16+ 는 적재 실패
numpy>=1.21.0
pandas>=1.3.0
fastmcp
pydantic>=2.0
