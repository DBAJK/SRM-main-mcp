"""LLM 오케스트레이터.

`agent/loop.py` 가 **파이썬이 순서를 정하고 LLM 은 판단만** 하는 고정 루프라면,
여기는 **LLM 이 도구를 직접 들고 한 스텝의 흐름을 스스로 잡는** 구조다.

    host.py     오케스트레이터 호스트. 스텝마다 `claude -p` 를 띄우고 봉투를 읽는다
    gateway.py  도구 게이트웨이. 서버 5개를 기동해 도구를 재노출하고 Guard·순서 기록·상한을 건다
    referee.py  심판. 스텝이 끝난 뒤 절차 위반을 판정해 기록한다 (막지 않는다)
    prompt.md   오케스트레이터 시스템 프롬프트. 의무와 규칙만 있고 호출 순서는 없다

두 구조는 같은 서버 · 같은 Guard · 같은 장부 · 같은 채점기를 쓴다. 다른 것은
"누가 다음 도구를 정하나" 하나이고, 그래서 서로의 대조군이 된다.
"""
